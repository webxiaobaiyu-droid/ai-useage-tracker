/**
 * 占位 dev 启动脚本。仅做 sleep + 打印，避免 electron-vite 报 no-script 错误。
 * 实际开发联调在后续任务里改成：concurrently vite + electron + electron-vite preview。
 */
console.log('[jusage-desktop] dev placeholder. Use `electron-vite dev` instead.');

