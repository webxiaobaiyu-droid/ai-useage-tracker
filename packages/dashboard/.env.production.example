# 生产构建可注入的环境变量示例（norepls 构建环境按需启用，勿直接复制为 .env.production）。
# 默认 `pnpm build` 面向 CLI 内置托管：VITE_API_TARGET=cli、根路径 `/`，无需任何覆盖。

# Mock 数据。生产环境应为 false。
# VITE_ENABLE_MOCK_DATA=false

# 自建站点时的静态资源前缀（默认 `/`）
# VITE_BASE=/aiusage/

# SPA 路由前缀（通常与 VITE_BASE 保持一致）
# VITE_ROUTER_BASE=/aiusage/

# 发 CDN：构建环境设 PUBLIC_PATH（优先于 VITE_BASE）
# PUBLIC_PATH=https://cdn.example.com/aiusage/

# 自建上传 API 根（无尾斜杠）。配合 VITE_API_TARGET=server 使用；
# 仅在你部署了自己的用量上报服务时才需要。
# VITE_API_BASE=https://api.example.com/aiusage_api
